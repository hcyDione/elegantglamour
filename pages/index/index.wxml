<!--index.wxml-->
<view class="container">
    <mychild image-src="{{imgsrc}}" bindmyevent="onMyEvent">
    </mychild>
    <slide slide-list="{{slidedata}}"></slide>
    <canvas canvas-id="clip" bindtap="doclup"></canvas>
    <view class="exp">图片裁剪(含像素比)</view>
    <view class="exp">
    	<button bindtap="doparse">打开wxparse解析</button>
    </view>
</view>
